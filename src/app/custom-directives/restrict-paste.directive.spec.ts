import { RestrictPasteDirective } from './restrict-paste.directive';

describe('RestrictPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictPasteDirective();
    expect(directive).toBeTruthy();
  });
});
